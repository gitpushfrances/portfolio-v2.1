{
  "installCommand": "npm install",
  "buildCommand": "npm run build && ls -la /vercel/path0/",
  "outputDirectory": "browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
