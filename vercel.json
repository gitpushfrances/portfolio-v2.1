{
  "installCommand": "npm install",
  "buildCommand": "npm run build && cp -r browser dist",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
